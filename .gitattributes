# Normalize all text files to use LF (Unix-style) line endings.
# This prevents issues when working on different operating systems.
* text=auto eol=lf

# Explicitly set shell scripts to have LF endings, as they will fail
# on Linux if they have CRLF (Windows-style) endings.
*.sh text eol=lf

.xml text eol=lf
.json text eol=lf